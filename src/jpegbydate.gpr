project JpegByDate is

   for Object_Dir use "./../bin";
   for Main use ("main.adb");
   for Source_Files use ("globals.ads", "inputs.adb", "inputs.ads", "main.adb", "outputs.adb", "outputs.ads");

   package Ide is
      for Vcs_Kind use "Git";
   end Ide;

   package Compiler is
   end Compiler;

   package Builder is
      for Executable ("main.adb") use "JpegByDate.exe";
   end Builder;

end JpegByDate;

