project JpegByDate is

   for Object_Dir use "./../bin";
   for Main use ("main.adb");
   for Source_Files use ("main.adb", "output.adb", "output.ads");

   package Ide is
      for Vcs_Kind use "Git";
   end Ide;

end JpegByDate;

